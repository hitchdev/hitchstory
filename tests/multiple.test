- name: Run through simple story
  tags:
    - story
    - simple
    - documentation
    - happy
  preconditions:
    files:
      example1.story: |
        Create file:
          scenario:
            - Create file
        Create file again:
          scenario:
            - Create file
      example2.story: |
        Create files:
          scenario:
            - Create file
  scenario:
    - Run command: |
        from hitchstory import StoryCollection, BaseEngine

        class Engine(BaseEngine):
            def create_file(self, filename="step1.txt", content="example"):
                with open(filename, 'w') as handle:
                    handle.write(content)

    - Assert exception:
        command: StoryCollection(".", Engine()).one()
        exception: MoreThanOneStory
    - Run command: |
        results = StoryCollection(".", Engine()).ordered_by_name().play()
        output(results.report())
    - Output is: |
        STORY RAN SUCCESSFULLY example1.story: Create file in 0.1 seconds.
        STORY RAN SUCCESSFULLY example1.story: Create file again in 0.1 seconds.
        STORY RAN SUCCESSFULLY example2.story: Create files in 0.1 seconds.

