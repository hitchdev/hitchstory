- Write out test successes/failures as they happen
- Fix rewriting on inherited story.
- Fix rewriting when a step parameter has a space in it.
- Fix inheritance from 3rd story down.
- Make exception code yellow not red.
- Light red for stacktrace -- add "suppress_stacktrace_for" message in template if in a step.
- Give line numbers to blue YAML snippets indicating failure.
- Rename expected_exception -> suppress_failure_stacktrace
- Properties Refactor:
- GivenProperties(
    python_version GivenProperty(schema, required is False, default is "(( python version ))", inherit_by is overwrite, default, merge properties, )
- ParameterProperties(
    python_version ParamProperty(
- ParameterProperties:
    ParameterProperty( a python 2 version, default 2.7.10, merge)
    StoryParameter(a python 3 version ,3.5.0
    StoryParameter("a valid browser", 
- Abstract story -- no steps or with a variable that is not tied down.
- Make it possible to put in a name of an unimplemented test and blank file.
- given equals ( "python version": Default(Str(), from_parameter"python version", default 3.5.0) 
- self.current_step.update(will_output=x) will fail if key is actually "will output"
- "Start line of YAML with list" -- why does it fail if you have not specified code?
- Fail with a more sensible error message when validators fail on steps.
- Print to screen or log to file while running.
- "Capture" mode and print mode (default)
- Params should match the schema of the precondition or step.
- Examples -- parameters via a story name (examples:)
- Counter examples -- known failures, params with a name.
- Use non-ascii characters in all tests.
- poststep method - self.poststep(self, previous_step)
- prestep method - self.prestep(self, following_step)
- group_setup(self, given_list) / group_teardown - run once when any group of stories is run - use for build? Does not work if build preconditions fail.
- kwargs story, demonstrate that conversion to underscore case does not happen in kwargs whereas it does in named args.
- *args story.
- hitchstory DEBUG mode.
